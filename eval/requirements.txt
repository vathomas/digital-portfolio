# Stage B Ragas eval — pinned dependencies for reproducible CI runs.
# Update via `pip install -U ...` then re-pin from `pip freeze`.
ragas==0.2.10
langchain-openai==0.2.14
datasets==3.2.0
openai==1.59.6
requests==2.32.3
